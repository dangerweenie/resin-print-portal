# Pinned 2026-08-22 to current stable, mutually-compatible releases
# (Flask 3.1.x requires Werkzeug >= 3.1). Provisional until verified on
# real target hardware — after installing on the Pi, run:
#   venv/bin/pip freeze > requirements.txt
# and commit the fully resolved (transitive-included) result as the source
# of truth for that hardware.
flask==3.1.3
werkzeug==3.1.8
gunicorn==26.1.0
